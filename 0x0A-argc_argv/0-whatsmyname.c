#include <stdio.h>

/**
 * main - prints its name, followed by newline.
 * @argc: argument count
 * @argv: argument vector
 *
 * Return 0
 */
int main(int argc, char **argv)
{
        (void)argc;

        printf("%\n", argv[0];
        return (0);
}
